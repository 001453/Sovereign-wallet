# Patch for tetherto/wdk-examples (root README.md)

Add this row to the **Examples** table (after `mcp-toolkit` or at end):

```markdown
| [browser-extension-sovereign-wallet](./browser-extension-sovereign-wallet/) | Multi-package | Chrome MV3 browser extension starter ([Sovereign Wallet](https://github.com/001453/Sovereign-wallet)) |
```

No `package.json` script changes required — this example is built from its own repository.
